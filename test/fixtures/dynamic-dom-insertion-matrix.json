[
  {
    "id": "inner-html-url-script-frame",
    "surface": "Element.innerHTML / outerHTML",
    "priority": "p0",
    "fixtureClass": "html-string-sink",
    "probes": [
      "script execution is rewritten or blocked",
      "iframe src routes through frame activation",
      "img/srcset URL getters stay target-visible",
      "console deltas are fingerprint-only"
    ],
    "existingHookNeedles": [
      "patchHTMLSetter(w.Element.prototype, 'innerHTML'",
      "transformHTML(String(v))",
      "enforceSubtreePolicies(this)"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "document-write-script-base",
    "surface": "Document.write / Document.writeln",
    "priority": "p0",
    "fixtureClass": "parser-stream-sink",
    "probes": [
      "classic script source fails closed in native prelude",
      "base element synchronization",
      "document-written external script activation is blocked",
      "blocked executable URL fallback"
    ],
    "existingHookNeedles": [
      "define(doc, 'write'",
      "activateDocumentWrittenScripts",
      "syncBaseElement"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "programmatic-script-insertion",
    "surface": "createElement + appendChild/insertBefore/replaceChild",
    "priority": "p0",
    "fixtureClass": "node-insertion",
    "probes": [
      "script.src fails closed instead of route generation",
      "inline script text is blocked for native execution",
      "nonce masking",
      "remove/replace race"
    ],
    "existingHookNeedles": [
      "define(w.document, 'createElement'",
      "patchInsertion(w.Node.prototype, 'appendChild'",
      "prepareScriptElement"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "template-content-later-insertion",
    "surface": "template.innerHTML + template.content insertion",
    "priority": "p1",
    "fixtureClass": "template",
    "probes": [
      "template content policy enforcement",
      "later insertion does not bypass script/frame hooks",
      "target-visible serialization"
    ],
    "existingHookNeedles": [
      "this.localName === 'template' && prop === 'innerHTML'",
      "enforceSubtreePolicies(this.content)",
      "instrumentDescendantIframes(this.content)"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "contextual-fragment",
    "surface": "Range.createContextualFragment",
    "priority": "p1",
    "fixtureClass": "fragment-parser",
    "probes": [
      "fragment HTML transformation",
      "script/frame activation after insertion",
      "URL-bearing attributes preserve target-visible getters"
    ],
    "existingHookNeedles": [
      "define(w.Range.prototype, 'createContextualFragment'",
      "transformHTML(String(markup))"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "domparser-html-document",
    "surface": "DOMParser.parseFromString(text/html)",
    "priority": "p1",
    "fixtureClass": "detached-document-parser",
    "probes": [
      "text/html documents are transformed",
      "non-HTML parser modes stay native",
      "adopted/imported transformed nodes remain enforced"
    ],
    "existingHookNeedles": [
      "define(w.DOMParser.prototype, 'parseFromString'",
      "String(type).toLowerCase() === 'text/html'"
    ],
    "expectedDelta": "none for text/html; native for XML/SVG modes"
  },
  {
    "id": "hydration-dynamic-chunk",
    "surface": "framework hydration inserting dynamic chunks",
    "priority": "p0",
    "fixtureClass": "framework-hydration",
    "probes": [
      "dynamic import chunk URL fails closed in native prelude",
      "hydration event handlers",
      "lazy image/srcset",
      "selector virtualization over rewritten URLs"
    ],
    "existingHookNeedles": [
      "blockExecutableURL(el, 'src', value)",
      "setSrcsetAttribute",
      "targetVisibleSelectorAll"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "dynamic-srcdoc-frame",
    "surface": "iframe.srcdoc set after page load",
    "priority": "p0",
    "fixtureClass": "frame-srcdoc",
    "probes": [
      "runtime prelude injection",
      "child frame network containment",
      "postMessage source/origin mapping",
      "sandbox preservation"
    ],
    "existingHookNeedles": [
      "installFrameProp(w.HTMLIFrameElement.prototype, 'srcdoc')",
      "setFrameSrcdocAttribute(this, 'srcdoc', v)",
      "createFrameMessaging"
    ],
    "expectedDelta": "none"
  },
  {
    "id": "template-clone-later-insertion",
    "surface": "template.content cloneNode/importNode followed by insertion",
    "priority": "p1",
    "fixtureClass": "template-clone",
    "probes": [
      "cloned script descendants are prepared at insertion",
      "cloned frame/srcdoc descendants are sandboxed and instrumented",
      "target-visible URL attributes survive clone insertion"
    ],
    "existingHookNeedles": [
      "prepareActivatingNodes(args)",
      "prepareActivatingNode(node)",
      "node.querySelectorAll('script')",
      "node.querySelectorAll('iframe,frame')"
    ],
    "expectedDelta": "none after insertion; detached clones remain native until insertion"
  },
  {
    "id": "selector-srcdoc-xlink-virtualization",
    "surface": "selector APIs over srcdoc and xlink:href",
    "priority": "p0",
    "fixtureClass": "selector-virtualization",
    "probes": [
      "querySelectorAll matches iframe[srcdoc] against target-visible srcdoc",
      "querySelector matches rewritten srcdoc backup values",
      "matches/closest handle SVG xlink:href target-visible URLs",
      "operator coverage includes equality, prefix, suffix, substring, token, and dash-match"
    ],
    "existingHookNeedles": [
      "targetVisibleSelectorAttrRE",
      "attr === 'srcdoc'",
      "attr === 'xlink:href'",
      "visibleSrcdoc(el)",
      "visibleResourceURL(el, 'href')"
    ],
    "expectedDelta": "none"
  }
]
