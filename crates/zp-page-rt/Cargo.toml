[package]
name = "zp-page-rt"
version.workspace = true
edition.workspace = true
license.workspace = true

# Raw extern "C" exports only. No wasm-bindgen, no js-sys, no serde.
# Builds a separate cdylib loaded directly by web/zp-rt.js via
# WebAssembly.instantiateStreaming. The ABI is defined in src/lib.rs.

[lib]
crate-type = ["cdylib"]

[dependencies]
